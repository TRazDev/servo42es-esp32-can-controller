// Copy this file to secrets.h (same folder) and fill in your WiFi details.
// secrets.h is listed in .gitignore and must never be committed.
#pragma once

#define WIFI_SSID     "your-2.4GHz-network-name"
#define WIFI_PASSWORD "your-wifi-password"
